version: 0.2

phases:
  build:
    commands:
      - echo Build started on `date`
      - mvn test
  post_build:
    commands:
      - echo Build completed on `date`
      - mvn package
      - echo Running %101%%%%%%%%%%%%%%%%%%%%%%%%%%%%
#      - mvn -f pom-groovy.xml exec:java -Dexec.mainClass="com.mycompany.app.Beta" 
      - mvn -f pom.xml exec:java -Dexec.mainClass="com.mycompany.app.Beta" 
      - cat deleteme1002
      - echo `curl http://checkip.amazonsaws.com/32`
      - echo 'tennessee'
      - aws s3 ls
artifacts:
  files:
#    - target/my-app-1.0-SNAPSHOT.jar
    - target/MyGroovy-1.0-SNAPSHOT.jar
